<p id="notice"><%= notice %></p>

<p>
  <strong>Tracking number:</strong>
  <%= @tracking.tracking_number %>
</p>

<p>
  <strong>Logistic:</strong>
  <%= @tracking.logistic %>
</p>
<% if @tracking.user == current_user %>
<%= link_to 'Edit', edit_tracking_path(@tracking) %> |
<% end %>
    <%= link_to 'Back', trackings_path %>
